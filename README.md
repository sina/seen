# Quick Dev Blogstro

![blogstro](/public/favicon-64x64-simple.png)



## 🧞 Commands

All commands are run from the root of the project. Use npm if preferred, from a terminal:

| Command                   | Action                                           |
| :------------------------ | :----------------------------------------------- |
| `pnpm install`             | Installs dependencies                            |
| `pnpm dev`             | Starts local dev server at `localhost:4321`      |
| `pnpm dev --host`      | local dev server w/exposed port from container etc      |
| `pnpm build`           | Build your production site to `./dist/`          |
| `pnpm preview`         | Preview your build locally, before deploying     |
| `pnpm preview --host`         | Preview your build locally, w/exposed port from container etc     |
| `pnpm astro ...`       | Run CLI commands like `astro add`, `astro check` |
| `pnpm astro -- --help` | Get help using the Astro CLI                     |
